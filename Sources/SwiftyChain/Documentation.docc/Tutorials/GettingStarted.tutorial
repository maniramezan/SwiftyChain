@Tutorial(time: 10) {
    @Intro(title: "Getting Started") {
        Use SwiftyChain to save and load a typed secret.
    }

    @Section(title: "Save a Token") {
        @ContentAndMedia {
            Create a `KeychainKey` and call the `Keychain` actor.
        }
    }
}
